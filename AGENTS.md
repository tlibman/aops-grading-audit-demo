# Retired deployment repository

The GitHub Pages reviewer site is retired. Do not deploy a reviewer website from this repository, add published batch data, enable GitHub Pages, add `.openai/hosting.json`, or use ChatGPT Sites unless the user explicitly reverses the local-only decision in the current conversation.

The active application is the local Mac app in `tlibman/aops-grading-audit`.
